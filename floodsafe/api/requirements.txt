# Runtime dependencies for the FastAPI service and the ingest job.
#
# Deliberately small. The geospatial build stack (rasterio, geopandas,
# WhiteboxTools, XGBoost) lives in floodsafe/pipeline/requirements.txt
# and is never needed to serve a request -- PostGIS does the spatial
# work at runtime, and the model's output is precomputed.

fastapi>=0.115
uvicorn[standard]>=0.32
psycopg[binary]>=3.2
psycopg_pool>=3.2
httpx>=0.27
