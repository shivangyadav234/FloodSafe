[pytest]
testpaths = tests
addopts = -q --strict-markers

# legacy/ holds the pre-rewrite routing prototypes and their tests. They
# import modules that no longer exist in the served application, so they
# are excluded rather than left to fail and mask real regressions.
norecursedirs = legacy floodsafe/data .localdb data
